#pragma once
#include "main.hpp"
#define RA_LINEAR 0
#define RA_MULTIPLE 1

void linearRegression(vector< vector<cv::Point> > points)
{
    double ax, ay; //average x, y coordinates;
    double dx, dy; //sum of deviaiton x, y
}
